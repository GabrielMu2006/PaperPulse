<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="PaperPulseGlassPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassFillBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource SurfaceCornerRadius}" />
        <Setter Property="Padding" Value="{StaticResource PanelPadding}" />
    </Style>

    <Style x:Key="PaperPulsePaperSurfaceStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PaperSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="#33FFFFFF" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource SurfaceCornerRadius}" />
        <Setter Property="Padding" Value="{StaticResource CardPadding}" />
    </Style>

    <Style x:Key="PaperPulseQuietButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="Background" Value="{StaticResource GlassFillBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Foreground="{TemplateBinding Foreground}"
                            Padding="{TemplateBinding Padding}" />
                    </Border>
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Target="Root.BorderBrush" Value="{StaticResource GlassStrokeStrongBrush}" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Target="Root.Opacity" Value="0.82" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Target="Root.Opacity" Value="0.42" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                        <VisualStateGroup x:Name="FocusStates">
                            <VisualState x:Name="Focused">
                                <VisualState.Setters>
                                    <Setter Target="Root.BorderBrush" Value="{StaticResource PulseMagentaBrush}" />
                                    <Setter Target="Root.BorderThickness" Value="2" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Unfocused" />
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource PaperPulseQuietButtonStyle}" />

    <Style x:Key="PaperPulseIconButtonStyle" TargetType="Button" BasedOn="{StaticResource PaperPulseQuietButtonStyle}">
        <Setter Property="Width" Value="{StaticResource CompactIconButtonSize}" />
        <Setter Property="Height" Value="{StaticResource CompactIconButtonSize}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="{StaticResource GlassFillBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style x:Key="PaperPulseProminentButtonStyle" TargetType="Button" BasedOn="{StaticResource PaperPulseQuietButtonStyle}">
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Background" Value="{StaticResource PulseAccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource PulseMagentaBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="White" />
    </Style>

    <Style x:Key="PaperPulseDestructiveButtonStyle" TargetType="Button" BasedOn="{StaticResource PaperPulseQuietButtonStyle}">
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Background" Value="#33F01A1F" />
        <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style x:Key="PaperPulseToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="Background" Value="{StaticResource GlassFillBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style TargetType="ToggleButton" BasedOn="{StaticResource PaperPulseToggleButtonStyle}" />

    <Style x:Key="PaperPulseInputStyle" TargetType="TextBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Background" Value="{StaticResource InputSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="PlaceholderForeground" Value="{StaticResource ShellSecondaryTextBrush}" />
        <Setter Property="SelectionHighlightColor" Value="#806B2BEB" />
        <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="TextBoxCommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="BorderElement.Background" Value="{StaticResource InputSurfacePointerOverBrush}" />
                                        <Setter Target="BorderElement.BorderBrush" Value="{StaticResource GlassStrokeStrongBrush}" />
                                        <Setter Target="ContentElement.Foreground" Value="{StaticResource ShellTextBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Focused">
                                    <VisualState.Setters>
                                        <Setter Target="BorderElement.Background" Value="{StaticResource InputSurfaceFocusedBrush}" />
                                        <Setter Target="BorderElement.BorderBrush" Value="{StaticResource InputBorderFocusedBrush}" />
                                        <Setter Target="BorderElement.BorderThickness" Value="2" />
                                        <Setter Target="ContentElement.Foreground" Value="{StaticResource ShellTextBrush}" />
                                        <Setter Target="PlaceholderTextContentPresenter.Foreground" Value="{StaticResource ShellTextBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="BorderElement.Background" Value="{StaticResource InputDisabledBrush}" />
                                        <Setter Target="BorderElement.Opacity" Value="0.55" />
                                        <Setter Target="ContentElement.Foreground" Value="{StaticResource ShellDisabledTextBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="TextBoxButtonStates">
                                <VisualState x:Name="ButtonCollapsed" />
                                <VisualState x:Name="ButtonVisible">
                                    <VisualState.Setters>
                                        <Setter Target="DeleteButton.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter
                            x:Name="HeaderContentPresenter"
                            Grid.ColumnSpan="2"
                            Content="{TemplateBinding Header}"
                            ContentTemplate="{TemplateBinding HeaderTemplate}"
                            Foreground="{StaticResource ShellSecondaryTextBrush}"
                            TextWrapping="Wrap"
                            Visibility="Collapsed" />
                        <Border
                            x:Name="BorderElement"
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            MinHeight="{TemplateBinding MinHeight}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Control.IsTemplateFocusTarget="True" />
                        <ScrollViewer
                            x:Name="ContentElement"
                            Grid.Row="1"
                            Margin="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            AutomationProperties.AccessibilityView="Raw"
                            Foreground="{TemplateBinding Foreground}"
                            HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                            HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                            IsTabStop="False"
                            VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                            VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                            ZoomMode="Disabled" />
                        <TextBlock
                            x:Name="PlaceholderTextContentPresenter"
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Margin="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            Foreground="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={StaticResource ShellSecondaryTextBrush}}"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding PlaceholderText}"
                            TextAlignment="{TemplateBinding TextAlignment}"
                            TextWrapping="{TemplateBinding TextWrapping}" />
                        <Button
                            x:Name="DeleteButton"
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="4"
                            Style="{StaticResource PaperPulseIconButtonStyle}"
                            Visibility="Collapsed"
                            AutomationProperties.AccessibilityView="Raw"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            FontSize="{TemplateBinding FontSize}"
                            IsTabStop="False">
                            <SymbolIcon Symbol="Clear" />
                        </Button>
                        <ContentPresenter
                            x:Name="DescriptionPresenter"
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Content="{TemplateBinding Description}"
                            Foreground="{StaticResource ShellSecondaryTextBrush}"
                            AutomationProperties.AccessibilityView="Raw"
                            x:Load="False" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PaperPulsePasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Background" Value="{StaticResource InputSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style x:Key="PaperPulseComboBoxStyle" TargetType="ComboBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{StaticResource InputSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="MaxDropDownHeight" Value="320" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <Style x:Key="PaperPulseComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style x:Key="PaperPulseNumberBoxStyle" TargetType="NumberBox">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{StaticResource InputSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource InputCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
    </Style>

    <Style x:Key="PaperPulseCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckBoxCommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="CheckSurface.BorderBrush" Value="{StaticResource GlassStrokeStrongBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Opacity" Value="0.5" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckBoxStates">
                                <VisualState x:Name="Unchecked" />
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter Target="CheckSurface.Background" Value="{StaticResource PulseAccentBrush}" />
                                        <Setter Target="CheckSurface.BorderBrush" Value="{StaticResource PulseMagentaBrush}" />
                                        <Setter Target="CheckGlyph.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Indeterminate">
                                    <VisualState.Setters>
                                        <Setter Target="CheckSurface.Background" Value="{StaticResource PulseAccentBrush}" />
                                        <Setter Target="IndeterminateGlyph.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckBoxFocusStates">
                                <VisualState x:Name="Focused">
                                    <VisualState.Setters>
                                        <Setter Target="CheckSurface.BorderBrush" Value="{StaticResource InputBorderFocusedBrush}" />
                                        <Setter Target="CheckSurface.BorderThickness" Value="2" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unfocused" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border
                            x:Name="CheckSurface"
                            Width="20"
                            Height="20"
                            Background="{StaticResource InputSurfaceBrush}"
                            BorderBrush="{StaticResource GlassStrokeBrush}"
                            BorderThickness="{StaticResource OnePixelThickness}"
                            CornerRadius="{StaticResource SmallCornerRadius}">
                            <Grid>
                                <SymbolIcon x:Name="CheckGlyph" Symbol="Accept" Foreground="White" Visibility="Collapsed" />
                                <SymbolIcon x:Name="IndeterminateGlyph" Symbol="Remove" Foreground="White" Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ContentPresenter
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Foreground="{TemplateBinding Foreground}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox" BasedOn="{StaticResource PaperPulseCheckBoxStyle}" />

    <Style x:Key="PaperPulseFormSectionStyle" TargetType="Border">
        <Setter Property="Background" Value="#0FFFFFFF" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource SurfaceCornerRadius}" />
        <Setter Property="Padding" Value="18" />
    </Style>

    <Style x:Key="PaperPulseDialogStyle" TargetType="ContentDialog">
        <Setter Property="Background" Value="{StaticResource MidnightBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GlassStrokeStrongBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource SurfaceCornerRadius}" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="PaperPulseListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource ShellTextBrush}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="PaperPulseMetadataPillStyle" TargetType="Border">
        <Setter Property="Background" Value="#22F01A1F" />
        <Setter Property="BorderBrush" Value="#33F01A1F" />
        <Setter Property="BorderThickness" Value="{StaticResource OnePixelThickness}" />
        <Setter Property="CornerRadius" Value="{StaticResource SmallCornerRadius}" />
        <Setter Property="Padding" Value="8,3" />
    </Style>
</ResourceDictionary>
